#include <iostream>
using namespace std;

int main() {
  //it is assigned a location in the computer’s memory
  int myVar;
  
  //the identifier is used to refer to the variable
  //the value of this variable is stored in the assigned location
  myVar=10;

  //the identifier is used to refer to the variable
  cout<<"myVar = "<<myVar<<endl;   
  
  return 0;
}